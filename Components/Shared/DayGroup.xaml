<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:shared="clr-namespace:Denly.Components.Shared"
             x:Class="Denly.Components.Shared.DayGroup">
    <VerticalStackLayout Spacing="8">
        <HorizontalStackLayout Spacing="8">
            <Label Text="{Binding DayHeader, Source={RelativeSource AncestorType={x:Type ContentView}}}"
                   FontSize="{x:Static shared:DesignTokens+Typography.SizeMd}"
                   TextColor="{x:Static shared:DesignTokens+Colors.DenShadow}" />
            <shared:UpdatedMark IsVisible="{Binding HasUpdates, Source={RelativeSource AncestorType={x:Type ContentView}}}" />
        </HorizontalStackLayout>
        <CollectionView ItemsSource="{Binding Events, Source={RelativeSource AncestorType={x:Type ContentView}}}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <shared:EventRow TimeText="{Binding TimeText}"
                                     Title="{Binding Title}"
                                     Location="{Binding Location}"
                                     ChildSummary="{Binding ChildSummary}"
                                     ChildAccentColor="{Binding ChildAccentColor}"
                                     IsUpdated="{Binding IsUpdated}" />
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentView>
